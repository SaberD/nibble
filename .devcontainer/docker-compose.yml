services:
  nibble:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    container_name: devcontainer-nibble
    network_mode: host
    user: vscode
    volumes:
      - ..:/workspace
    working_dir: /workspace
    command: sleep infinity
    stdin_open: true
    tty: true
